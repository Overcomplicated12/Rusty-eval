# AI-2026-023

## Metadata

Date: 2026-08-16
Time: 2026-08-16T19:56:15-04:00
Tool: Codex
Model: GPT-5
Researcher: Not recorded
Purpose: Implement V3 Python inventory runner translation-unit selection and per-TU Clang scanner orchestration

## Exact human-authored prompt

Update the V3 Python inventory runner so the user can scan a directory instead of manually specifying one translation unit at a time. Use compile_commands.json as the authoritative TU list, filter its entries to those under the requested directory, then invoke the existing clang_inventory executable once per matching TU with --source-file, combining successful JSONL output and recording per-TU failures. Keep scope/dedup logic separate from this runner; this change should only handle TU selection, scanner invocation, and failure collection.

## AI output usage

Used:
- Yes

Description:

Implemented V3 compile-database directory selection, one scanner invocation per
selected translation unit from its recorded compile-command directory, raw JSONL
aggregation, and per-TU failure capture.

## Research artifacts affected

Files:

- `src/v3/cpp_inventory/runner.py`
- `src/v3/cpp_inventory/cli.py`
- `src/v3/cpp_inventory/report.py`
- `tests/v3/cpp_inventory/test_cli.py`

Git commit:

Pull request:

## AI-generated code

AI-generated code present:
- Yes

Files / functions / regions:

- `runner.py`: TU selection and scanner orchestration
- `cli.py`: real scan command arguments and result writing
- `report.py`: raw scanner artifact writer
- `test_cli.py`: directory/filter/failure regression coverage

The retained runner correction uses each selected compile-command entry's
`directory` as the scanner process working directory, so relative module-map
and generated-module paths resolve correctly.

The retained scanner correction expands CMake response files and, for a
single-source scan, selects a compile command whose response files are present
instead of running stale duplicate commands.

## Human contribution and review

Describe:

- what the researcher independently decided
- what was reviewed
- what was changed manually
- what AI suggestions were rejected

The researcher requested the runner behavior and must review retained code
before use in a research run. The implementation intentionally does not apply
scope filtering or deduplication.

## Disclosure status

Explicit citation/disclosure required:
- Pending researcher determination

Reason:

This is AI-assisted implementation of research evaluation tooling.

## Notes

Verification: V3 unittest suite passed (4 tests); Python bytecode compilation
passed; the Clang scanner CMake build passed. The `pytest` module was
unavailable in this environment. Mako RAFT parsing was partially validated:
the local command-execution window terminated an otherwise-running individual
translation-unit scan after 30 seconds.
