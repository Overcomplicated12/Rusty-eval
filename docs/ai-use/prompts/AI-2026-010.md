# AI-2026-010

## Metadata

Date: 2026-08-10
Time: 2026-08-10T14:32:32-04:00
Tool: Codex
Model: GPT-5
Researcher: Not recorded
Purpose: Build Phase 2 automated RustyCpp migration harness scaffolding and record all AI-assisted actions

## Exact human-authored prompt

You are working in:

    ~/projects/rusty-eval

Build the boilerplate for Phase 2: an automated RustyCpp migration harness.

Phase 1 = inventory / feasibility.
Phase 2 = automatically attempt C++ -> RustyCpp inline-Rust DSL migration, validate it, and measure token + human effort.

This task is scaffolding only.

DO NOT:
- migrate real code yet
- modify Mako, Arrow, Protobuf, ZeroMQ, or RustyCpp
- run real Codex migration jobs
- run real benchmarks
- modify target repositories
- generate research conclusions

Before editing:
1. Read AGENTS.md.
2. Inspect existing Phase 1 code/tests/docs.
3. Reuse existing conventions.
4. Preserve existing AI-use/provenance rules.

Target architecture:

Phase 1 inventory
      |
      v
MigrationUnit
      |
      v
clean git worktree
      |
      v
baseline build/test
      |
      v
coding agent
      |
      v
patch-scope validation
      |
      v
RustyCpp
      |
      v
build
      |
      v
tests
      |
      +---- failure -> repair loop
      |
      v
effort/result logging

The harness controls the experiment.
The coding agent only proposes conversions/repairs.

Create roughly:

rusty-eval/
├── harness/
│   ├── __init__.py
│   ├── cli.py
│   ├── models.py
│   ├── config.py
│   ├── experiment.py
│   ├── workspace.py
│   ├── selection.py
│   ├── context.py
│   ├── patch.py
│   ├── rustycpp.py
│   ├── build.py
│   ├── testing.py
│   ├── benchmarking.py
│   ├── repair.py
│   ├── telemetry.py
│   ├── human.py
│   ├── reporting.py
│   ├── agents/
│   │   ├── base.py
│   │   └── codex_cli.py
│   └── prompts/
│       ├── initial_conversion.md
│       ├── transpiler_repair.md
│       ├── compile_repair.md
│       └── test_repair.md
├── configs/
│   └── example.toml
├── selections/
│   └── example.json
├── tests/harness/
├── docs/
│   ├── phase2-harness.md
│   ├── phase2-methodology.md
│   └── human-effort.md
└── scripts/
    └── summarize_results.py

Do not create useless empty files; useful stubs are fine.

Implement these core pieces:

1. models.py
Define:
- ExperimentState:
  CREATED, PREPARING, BASELINING, CONTEXT_BUILDING,
  CONVERTING, TRANSPILING, BUILDING, TESTING,
  REPAIRING, HUMAN_REQUIRED, BENCHMARKING,
  COMPLETED, FAILED
- AttemptPurpose:
  INITIAL_CONVERSION, TRANSPILER_REPAIR,
  COMPILE_REPAIR, TEST_REPAIR
- TokenSource:
  PROVIDER_REPORTED, LOCAL_ESTIMATE, UNAVAILABLE

Models:
- MigrationUnit
- TokenUsage
- CommandResult
- AgentRequest / AgentResponse
- AgentAttempt
- RustyCppAttempt
- BuildAttempt
- TestAttempt
- BenchmarkResult
- HumanIntervention
- ExperimentResult

Make them JSON-serializable.

2. config.py
Load TOML configs using stdlib tomllib if possible.

Example fields:
- application repo/name/commit
- source roots
- RustyCpp repo/commit/command
- build command
- targeted/full test commands
- benchmark command
- workspace root
- attempt budgets
- edit policy

Validate malformed configs early.

3. workspace.py
Support isolated git worktrees:
- create
- verify commit
- get changed files
- get/save diff
- cleanup

Tests must use temporary fake git repos, not real research repos.

Never reset/clean the canonical checkout.

4. selection.py
Support MigrationUnit from:
- explicit input
- selections/example.json

Do not implement automatic sampling yet.

5. context.py
Simple deterministic context builder:
- target source lines
- configurable surrounding lines
- metadata
Do not build an AST engine yet.

6. agents/
Create abstract AgentBackend.

codex_cli.py should only implement command construction/dry-run hooks for now.
DO NOT actually invoke Codex in this task.

7. prompts/
Create versioned templates:
- initial conversion
- transpiler repair
- compile repair
- test repair

Rules should include:
- modify only allowed files
- preserve behavior/interfaces
- no test/benchmark edits
- no RustyCpp edits
- no generated-code hand editing
- no weakened assertions/diagnostics
- harness independently decides success

Use prompt_version = "phase2-v1".

8. patch.py
Programmatically validate changed files.
Reject unauthorized edits such as tests, benchmarks, build config, generated code, or unrelated files unless explicitly allowed.

9. rustycpp.py
Create a runner interface.
Command must come from config.
Capture command, stdout/stderr, exit code, time.
Do not guess the RustyCpp CLI.

10. build.py / testing.py
Run configured commands and return structured results.

11. repair.py
Implement retry-budget logic only.

Suggested defaults:
- max_total_attempts = 7
- transpiler repairs = 2
- compile repairs = 2
- test repairs = 2

Return AUTOMATION_EXHAUSTED when limits are reached.

12. telemetry.py
Track:
- input/output/total tokens
- token source
- agent wall time
- RustyCpp/build/test time
- attempts by type

Never fabricate token counts.

13. human.py
Support recording migration-specific manual effort.

Categories:
- LOCAL_REFACTOR
- INTERFACE_REFACTOR
- OWNERSHIP_REFACTOR
- UNSAFE_BOUNDARY
- RUSTYCPP_WORKAROUND
- OTHER

Clearly document:
migration effort != harness debugging effort.

14. reporting.py
Write per-experiment artifacts like:

results/<app>/<experiment-id>/
├── manifest.json
├── result.json
├── environment.json
├── unit.json
├── baseline/
├── attempts/001/
├── human/
└── final/

Never overwrite failed attempts.

15. experiment.py
Create a dry-run/mock-capable state machine.

Eventual flow:

workspace
-> baseline build/test
-> context
-> agent
-> patch validation
-> RustyCpp
-> build
-> targeted tests
-> repair or success

Do not run a real migration yet.

16. cli.py
Support at least:

python -m harness.cli validate-config --config configs/example.toml

python -m harness.cli show-config --config configs/example.toml

python -m harness.cli dry-run \
  --config configs/example.toml \
  --selection selections/example.json

python -m harness.cli show-unit \
  --selection selections/example.json \
  --unit example-001

17. Tests
Add tests for:
- models/serialization
- config validation
- worktree creation/cleanup
- patch scope
- state transitions
- retry exhaustion
- telemetry aggregation
- reporting

Use only temporary/fake repos.

18. Documentation
docs/phase2-harness.md should include Mermaid diagrams for:

Phase relationship:

```mermaid
flowchart TD
    A[Phase 1 Inventory] --> B[Migration Unit]
    B --> C[Phase 2 Harness]
    C --> D[Coding Agent]
    D --> E[RustyCpp]
    E --> F[Build]
    F --> G[Tests]
    G --> H[Effort Results]

Harness architecture:

Agent/controller split:

docs/phase2-methodology.md should define:

Automatic success:

RustyCpp passes
build passes
required tests pass
no human intervention

Human-assisted success:

same, after recorded human intervention

Failure:

attempt budget exhausted or blocker remains

Automated effort:

agent calls
tokens
agent time
retries

Human effort:

manually timed migration-specific work only
README / AGENTS.md
Add concise Phase 2 documentation as needed.
Do not duplicate existing rules.
Provenance
Use the existing AI-use logging procedure.
Preserve this exact prompt verbatim.
Record that Codex generated/modified Phase 2 infrastructure code.
Do not fabricate token usage.

Implementation requirements:

Python 3.11+
standard library preferred
type hints
pathlib
subprocess without shell=True
deterministic JSON where practical
small testable functions
no giant framework

Do NOT implement:

real migrations
multi-agent systems
dashboards
Docker
model comparison
automatic PRs
automatic RustyCpp edits
AST context system
benchmark statistics

Acceptance criteria:

existing Phase 1 tests still pass
new harness tests pass
validate-config works
show-config works
dry-run works without modifying real repos
worktree tests pass
patch-scope tests pass
retry exhaustion is tested
token source distinctions are tested
ExperimentResult round-trips through JSON
documentation diagrams are present
AI-use log is updated

Run:
python -m pytest
python -m compileall harness tests

Run ruff only if already configured/available.

Additional delivered instruction:

Log this prompt and any actions according to the instructions in this repository

At the end report:

what you implemented
final Phase 2 file tree
files created/modified
tests and results
intentional stubs
next milestone

## AI output usage

Used:
- Yes

Description: Codex-generated Phase 2 harness scaffolding was retained. It
implements only deterministic configuration, dry-run, temporary-worktree,
command-wrapper, telemetry, reporting, and test infrastructure. No target
repository, RustyCpp installation, coding agent, benchmark, or migration was
run.

## Research artifacts affected

Files:
- `harness/`, including agent dry-run hooks and versioned prompts
- `configs/example.toml`, `selections/example.json`, and `scripts/summarize_results.py`
- `tests/harness/`
- `docs/phase2-harness.md`, `docs/phase2-methodology.md`, `docs/human-effort.md`, and `README.md`
- this AI-use record, `docs/ai-use/log.md`, and `data/ai-use-index.csv`

Git commit: Not created.

Pull request: Not created.

## AI-generated code

AI-generated code present:
- Yes

Files / functions / regions: Phase 2 models, TOML validation, isolated
worktree lifecycle, selection/context, patch validation, configured command
wrappers, retry accounting, telemetry aggregation, human-effort records,
append-only reporting, dry-run controller/CLI, example inputs, and synthetic
tests.

## Human contribution and review

Describe:

- The researcher supplied and bounded the requested architecture, safety
  constraints, methodology, and acceptance criteria.
- Codex inspected the existing Phase 1 conventions and completed synthetic
  validation: 9 existing inventory tests and 9 harness tests passed; the CLI
  validate/show/dry-run commands and `compileall` passed. Ruff was unavailable.
- Researcher review of retained AI-generated code is pending.
- A potentially destructive modified-worktree cleanup was rejected as a
  default: cleanup now requires explicit `force=True` after callers preserve a
  diff artifact.

## Disclosure status

Explicit citation/disclosure required:
- Pending researcher determination

Reason: Project policy requires traceable disclosure and researcher review for
significant AI-generated implementation assistance.

## Notes

No experimental telemetry, research findings, or claims about migration
success were generated.
