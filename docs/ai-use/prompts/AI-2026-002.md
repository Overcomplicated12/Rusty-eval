# AI-2026-002

## Metadata

Date: 2026-08-08
Time: 2026-08-08T23:41:58-04:00
Tool: Codex
Model: unknown
Researcher: Not recorded
Purpose: Initialize the rusty-eval repository and evaluation infrastructure

## Exact human-authored prompt

Set up a new repository named rusty-eval for evaluating RustyCpp migrations across multiple applications such as SQLite, Mako Raft, Masstree, and future targets.

This repository will contain the reusable inventory scanner, automated conversion harness, experiment metadata, and generated evaluation results. Target applications themselves should remain separate repositories under ~/projects/.

Do not modify SQLite, Mako, Masstree, or rusty-cpp as part of this setup.

1. Inspect environment first

Before making changes, verify:

pwd
git --version
gh --version
gh auth status
python3 --version
clang --version

We expect the workspace to eventually look like:

~/projects/
├── rusty-eval/
├── sqlite/
├── mako/
├── rusty-cpp/
└── masstree/        # possibly later

If GitHub CLI is not authenticated, do not ask for or handle credentials yourself. Stop and tell me to run:

gh auth login

Then wait for me to authenticate before continuing GitHub operations.

2. Create the local repository

If ~/projects/rusty-eval does not exist:

mkdir -p ~/projects/rusty-eval
cd ~/projects/rusty-eval
git init -b main

If it already exists, inspect it instead of overwriting anything.

Verify:

git status
git remote -v

Do not modify global Git configuration.

If this repository lacks a Git author identity, check existing Git configuration and tell me what is missing before changing it.

3. Create the initial repository structure

Set up:

rusty-eval/
├── AGENTS.md
├── README.md
├── .gitignore
├── pyproject.toml
│
├── inventory/
│   ├── __init__.py
│   ├── inventory.py
│   ├── models.py
│   └── rules.py
│
├── harness/
│   ├── __init__.py
│   └── README.md
│
├── configs/
│   └── README.md
│
├── results/
│   └── README.md
│
├── tests/
│   ├── fixtures/
│   └── README.md
│
└── docs/
    ├── methodology.md
    └── experiment-schema.md

Do not implement the full inventory scanner yet. Placeholder Python modules are fine, but they should have useful module docstrings and not pretend functionality exists.

4. Write AGENTS.md

AGENTS.md is important because Codex/APAS/other agents will work in this repository later.

Write it as the authoritative operating instructions for agents.

Include at least the following:

# AGENTS.md

## Project purpose

rusty-eval is the evaluation and automation repository for RustyCpp
application migrations.

It is intended to measure:
- migratability
- automatic conversion success
- correctness
- performance overhead
- token usage
- agent wall-clock time
- build/test attempts
- human interventions
- human intervention time
- converted LOC

Target applications such as SQLite, Mako, and Masstree live in separate
repositories. Do not vendor or silently modify them from this repository.

## Repository responsibilities

inventory/
    Static inventory and migration-feasibility analysis.

harness/
    Automated conversion/build/test/repair orchestration.

configs/
    Per-application experiment definitions.

results/
    Machine-generated experiment results.

tests/
    Tests for rusty-eval itself.

docs/
    Methodology and experiment definitions.

## Core research rules

1. Reproducibility is more important than convenience.
2. Every application evaluation must record the exact source commit.
3. Record the RustyCpp/transpiler commit used for every experiment.
4. Never silently change methodology between applications.
5. Preserve failed conversion attempts and failure reasons.
6. Measure token usage and human intervention rather than estimating them.
7. Do not claim code is unmigratable merely because a heuristic identifies
   a difficult construct.
8. Separate:
      - directly convertible
      - reshape/refactor required
      - blocked by a transpiler capability
      - intentional unsafe/ABI boundary
      - unknown
9. Classify difficult code by reason, not merely by file.
10. Inventory work must not modify the target application's source.

## Target repositories

Expected sibling layout:

~/projects/rusty-eval
~/projects/sqlite
~/projects/mako
~/projects/rusty-cpp
~/projects/masstree

Do not assume all targets exist.
Detect and report missing repositories.

## Inventory rules

The inventory scanner must be deterministic.

Prefer Clang AST information for declarations/types and use textual
analysis only where appropriate.

The standard inventory buckets are:

- TRIVIAL
- REFACTOR_THEN_DSL
- NEEDS_TRANSPILER
- BOUNDARY
- UNKNOWN

Each classification should contain:

- bucket
- primary_reason
- secondary_reasons
- confidence

UNKNOWN is preferable to an unsupported confident classification.

Do not equate raw pointers, goto, unions, callbacks, macros, or other
individual constructs with automatic impossibility.

## Target-source safety

During inventory:

- do not modify target source
- do not run automatic migrations
- do not commit inside target repos

During later conversion experiments:

- operate on explicit experiment branches/worktrees
- preserve the pinned baseline
- make all mutations attributable to a recorded run

## Generated results

Generated per-application results should live under:

results/<application>/<run-or-inventory-id>/

Machine-readable data should prefer JSON/CSV.

Human-readable summaries should be Markdown.

Do not manually alter generated results to make an experiment look better.

## Git discipline

- Keep commits small and descriptive.
- Do not commit build directories.
- Do not commit credentials or API tokens.
- Do not force-push unless explicitly instructed.
- Do not modify unrelated repositories.
- Do not automatically merge PRs.
- Before committing, inspect `git diff` and `git status`.

## Secrets

Never place:
- GitHub tokens
- API keys
- authentication cookies
- private SSH keys
- model-provider credentials

in the repository.

Use existing authenticated CLIs/environment configuration instead.

## Agent behavior

Before significant implementation:

1. inspect relevant files
2. summarize the proposed change
3. identify methodological implications
4. implement the smallest coherent change
5. run relevant tests
6. inspect the diff
7. report results accurately

Do not begin a large-scale application conversion unless explicitly
instructed.

Improve the wording and organization if appropriate, but preserve all of those rules.

5. Create .gitignore

Include at least:

__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

build/
dist/
*.egg-info/

.DS_Store
.idea/

# Local experiment scratch
scratch/
tmp/

# Secrets / local environment
.env
.env.*
!.env.example

# Generated large/raw logs
results/**/logs/

Do not blindly ignore all results/, because summary results are intended to be versionable.

6. README

Create a concise but useful README.md explaining:

what RustyCpp evaluation is
why this repo exists
target applications remain separate
current phase is inventory/feasibility
later phases will perform automated conversion and measure correctness, performance, tokens, and human effort
directory structure
basic usage placeholder

Make clear that this repository is research tooling, not RustyCpp itself.

7. Methodology document

In docs/methodology.md, define this high-level pipeline:

pinned target source
        ↓
inventory
        ↓
migratability report
        ↓
automated conversion harness
        ↓
build / test / repair loop
        ↓
correctness evaluation
        ↓
performance evaluation
        ↓
effort accounting
        ↓
results

Explain that the methodology should be frozen before comparing multiple applications so they receive comparable treatment.

8. Experiment schema

Create an initial docs/experiment-schema.md describing fields we expect to eventually collect:

application
application_commit
rustycpp_commit
harness_commit
compiler
machine/environment

source_loc
eligible_loc
converted_loc

input_tokens
output_tokens
total_tokens

agent_wall_time
conversion_attempts
build_attempts
test_attempts

human_interventions
human_minutes

baseline_tests
converted_tests

baseline_runtime
converted_runtime

result
failure_reason

This is preliminary; don't overengineer the implementation yet.

9. Python project setup

Create a minimal modern Python project configuration in pyproject.toml.

We expect Python 3.11+.

Configure at least:

pytest
ruff

Avoid unnecessary dependencies at this stage.

Do not install a large framework.

Add a minimal test proving the package can be imported if useful.

10. Verify repository

Run:

python3 -m compileall inventory harness
git status
git diff

Run any tests you add.

Fix obvious setup problems.

11. GitHub setup

Once local setup is complete, check:

gh auth status

If authenticated and there is no origin, create a private GitHub repository named rusty-eval under my currently authenticated GitHub account:

gh repo create rusty-eval \
  --private \
  --source=. \
  --remote=origin

Do not create a duplicate if one already exists.

If a remote rusty-eval repository already exists under my account, connect this local repository to it instead.

Verify:

git remote -v
gh repo view

Do not expose any credentials.

12. Initial commit

Inspect all files and make one initial commit:

Initialize RustyCpp evaluation harness

Before committing:

git status
git diff --check

Then push:

git push -u origin main

Verify:

git status
git log --oneline -5
gh repo view
13. Final report

When finished, report:

local repository path
GitHub repository name
GitHub remote
current branch
commit SHA
files created
tests/checks run
whether push succeeded
any authentication/configuration issue

Do not start implementing the SQLite inventory scanner after setup.

Stop once the repository itself is completely initialized, documented, connected to GitHub, committed, and pushed.

## AI output usage

Used:
- Yes

Description: The initial repository structure and documentation were created for this task. This record was added later because provenance logging was not yet available at the time of the setup.

## Research artifacts affected

Files: initial repository files, including AGENTS.md, README.md, .gitignore, pyproject.toml, inventory/, harness/, configs/, results/, tests/, and docs/

Git commit:

Pull request:

## AI-generated code

AI-generated code present:
- Yes

Files / functions / regions: placeholder Python package modules and `tests/test_imports.py` created during the repository setup.

## Human contribution and review

Describe: The user supplied the requirements. This historical record does not assert details about subsequent researcher review or manual modifications that were not recorded.

- what the researcher independently decided
- what was reviewed
- what was changed manually
- what AI suggestions were rejected

## Disclosure status

Explicit citation/disclosure required:
- TBD

Reason:

## Notes
