# AI-2026-022

## Metadata

Date: 2026-08-14
Time: 2026-08-14T00:40:21-04:00
Tool: Codex
Model: GPT-5
Researcher: Not recorded
Purpose: Create isolated V3 C++ inventory boilerplate for future Clang-backed production-function discovery.

## Exact human-authored prompt

Work in ~/projects/rusty-eval.

Create a simple boilerplate for a completely separate V3 C++ inventory tool.

Important:
- Do NOT modify or reorganize the existing inventory code.
- Keep V3 isolated so the older inventory tooling stays untouched.
- Same rule for results: all V3 outputs must go under a dedicated V3 results folder.
log this prompt
- I will implement the real parser and classification logic manually later.

Create roughly this structure:

src/v3/
    __init__.py
    cpp_inventory/
        __init__.py
        cli.py
        config.py
        scope.py
        dedup.py
        report.py
        schema.py

tools/v3/
    clang_inventory/
        CMakeLists.txt
        main.cpp

configs/v3/
    cpp-inventory/
        leveldb.toml

tests/v3/
    cpp_inventory/

results/v3/
    cpp-inventory/

Goal:
Set up the skeleton for a future parser-backed scanner that will eventually use Clang AST information to produce a trustworthy production-function denominator.

For now, do NOT fully implement:
- AST parsing
- migration classification
- complex deduplication
- RustyCpp feasibility heuristics

Implement only enough boilerplate so the pieces connect cleanly.

1. CLI

Support something like:

python -m v3.cpp_inventory.cli scan     --config configs/v3/cpp-inventory/leveldb.toml

For now the command can:
- load config
- create a V3 output directory
- call placeholder scan functions
- write placeholder summary/report files

All results must go under:

results/v3/cpp-inventory/<project>/<run-id>/

2. Config

Use TOML.

Support simple fields for:
- project name
- repo path
- compile_commands.json path
- include paths
- exclude paths

3. Schema

Define a simple dataclass or TypedDict for a future function record with fields like:

- id
- usr
- qualified_name
- kind
- file
- start_line
- end_line
- is_definition
- is_implicit
- is_template
- is_template_instantiation
- is_lambda

Keep this minimal.

4. Clang tool

Create a minimal CMake project and main.cpp under tools/v3/clang_inventory/.

It only needs to:
- compile cleanly if Clang tooling is available
- accept basic command-line arguments
- contain clear TODO comments for future AST traversal and JSONL output

Do not implement the full LibTooling scanner yet.

5. Scope and dedup

Create placeholder functions with simple interfaces, for example:

apply_scope(...)
deduplicate_functions(...)

They can currently pass data through unchanged.

6. Reporting

Create basic writers for:

results/v3/cpp-inventory/<project>/<run-id>/
    manifest.json
    coverage.json
    functions.jsonl
    parse_failures.jsonl
    summary.json
    report.md

For now these can contain basic metadata and empty counts.

7. Tests

Add a few small tests for:
- config loading
- CLI setup
- scope placeholder behavior
- dedup placeholder behavior

8. Keep V3 isolated

Do not:
- modify src/inventory/
- modify old result directories
- move existing files
- reuse old output paths
- add RustyCpp migration categories
- add migratability heuristics
- add unnecessary dependencies
- overengineer abstractions

The point of V3 is to have a clean new area that can evolve independently without making the existing inventory implementation messier.

At the end, summarize:
- files created
- what currently works
- what remains TODO
- confirm that no existing inventory code or old results were modified

## AI output usage

Used:
- Yes

Description:

Created isolated V3 C++ inventory boilerplate, V3-only placeholder output,
focused tests, and a minimal Clang command-line executable. Existing inventory
code and old result directories were not changed.

## Research artifacts affected

Files:

- `src/v3/`
- `tools/v3/clang_inventory/`
- `configs/v3/cpp-inventory/leveldb.toml`
- `tests/v3/cpp_inventory/`
- `results/v3/cpp-inventory/`

Git commit:

Pull request:

## AI-generated code

AI-generated code present:
- Yes

Files / functions / regions:

- V3 scaffold files listed above

## Human contribution and review

Describe:

- what the researcher independently decided
- what was reviewed
- what was changed manually
- what AI suggestions were rejected

The researcher will implement parser traversal, scope semantics,
deduplication, and feasibility classification manually later.

## Disclosure status

Explicit citation/disclosure required:
- Pending researcher determination

Reason:

The work creates evaluation tooling and methodology infrastructure.

## Notes

Verification: legacy suite (23 tests), V3 suite (4 tests), V3 CLI smoke run,
Python compilation, and CMake configure/build/run with Clang 22 all passed.
