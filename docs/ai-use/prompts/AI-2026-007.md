# AI-2026-007

## Metadata

Date: 2026-08-09
Time: 2026-08-09T00:48:02-04:00
Tool: Codex
Model: gpt 5.6 terra
Researcher: Zeyu Wang
Purpose: Implement inventory methodology version 2 from the SQLite review findings

## Exact human-authored prompt

Focus only on verifying and improving the inventory methodology before any Phase 2 conversion work.

Use the findings from AI-2026-006 to implement inventory methodology version 2.

Do not modify SQLite source, do not perform any RustyCpp conversion, and do not implement Phase 2 yet.

Fix at least:

static_local false positives caused by file/function-level static
flexible_array false positives from parameters, comments, and ordinary arrays
file-wide conditional_compilation
file-wide macro_use
unreachable/useless UNKNOWN
overly broad extern => BOUNDARY
feature matching inside comments and strings

Preserve methodology v1 and its SQLite results unchanged.

Add adversarial tests covering:

true local static
file-static function
file-static global
true flexible-array member
array parameter x[]
ordinary fixed array
comments containing [], goto, union, memcpy, etc.
strings containing code-like text
nested #if
declaration under #if
unrelated #if elsewhere in a file
macro-generated declaration
ordinary macro use inside a function
extern declaration that is not necessarily an ABI boundary

Make UNKNOWN reachable for declarations the lexical scanner cannot classify confidently.

After implementing v2:

run all synthetic/adversarial tests;
do not run the full SQLite inventory yet;
report the exact v2 classification rules and differences from v1;
identify any remaining lexical-scanner weaknesses;
stop for researcher approval.

Log this prompt in the AI-use log.

## AI output usage

Used:
- Yes

Description: Implemented and retained a separate inventory methodology-v2 scanner, rules, documentation, and adversarial tests. Methodology v1 and its SQLite outputs were not altered; no SQLite scan or conversion was run.

## Research artifacts affected

Files: `inventory/inventory_v2.py`; `inventory/rules_v2.py`; `inventory/models.py`; `inventory/inventory.py`; `tests/fixtures/inventory_v2_adversarial.c`; `tests/test_inventory_v2.py`; `README.md`; `docs/methodology.md`; `AGENTS.md`

Git commit:

Pull request:

## AI-generated code

AI-generated code present:
- Yes

Files / functions / regions: methodology-v2 scanner/rule modules and adversarial tests.

## Human contribution and review

Describe: The researcher directed the v2 changes from the v1 review. Synthetic and adversarial tests verified static scope, flexible members, comment/string sanitization, scoped preprocessing, macro-generated UNKNOWN, and non-boundary extern behavior. Researcher approval is pending before any SQLite v2 run.

- what the researcher independently decided
- what was reviewed
- what was changed manually
- what AI suggestions were rejected

## Disclosure status

Explicit citation/disclosure required:
- TBD

Reason:

## Notes
